/**
 * Simple Sequential Implementation of the K-Means Lloyds Algorithm
 */
#include "config.h"
#include "log.h"

void run_rooms(int* cost_matrix, int num_persons, int num_processes)
{
    INFO("NOOP run_rooms with num_persons=%d num_processes=%d", num_persons, num_processes);
}

void finalize_rooms()
{
    INFO("NOOP finalize_rooms");
}
